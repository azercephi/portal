

function dispTag(timeStamp) {
    // haha = getRecommandation(timeStamp);

    var btn = document.createElement("BUTTON");
    var t = document.createTextNode("hello");
    btn.appendChild(t);
    document.body.appendChild(btn);
}